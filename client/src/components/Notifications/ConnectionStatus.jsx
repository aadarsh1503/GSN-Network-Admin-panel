import React from 'react';
import { useGlobalNotifications } from '../../contexts/GlobalNotificationContext';

const ConnectionStatus = () => {
  // Don't show any connection status - completely hidden
  return null;
};

export default ConnectionStatus;